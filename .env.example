# SMTP (example for Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=youraccount@gmail.com
SMTP_PASS=your-app-password

# Optional: custom from address shown in mail
FROM_EMAIL=youraccount@gmail.com

# Optional
PORT=3000
